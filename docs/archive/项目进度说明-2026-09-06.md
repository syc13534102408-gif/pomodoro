# 松果 / 专注时光 · 项目进度说明（2026-09-06）

> **⚠️ 已归档（2026-09-08）**：进度快照已过时（目录名、UI 体系、测试数量、待办状态均已变化）。
> 当前状态与待办见 [`../../README.md`](../../README.md) 与 [`../../docs/07-常见问题与注意事项.md`](../../docs/07-常见问题与注意事项.md)。

> 配套《项目交接文档.md》阅读。本文聚焦「当前目录结构、各端状态、进行中事项与待办」。

## 1. 目录结构

```
wo-xi/                                  # 仓库根（GitHub: syc13534102408-gif/pomodoro）
├── index.html                          # ★ 网页版全部代码（单文件 + 内联 JS）
├── sw.js                               # 网页端 Service Worker（后台提醒用）
├── README.md                           # 部署要点速记
├── 项目交接文档.md                      # 完整交接文档（架构/命令/雷区）
├── 项目进度说明-2026-09-06.md           # 本文档
├── pomodoro_app/                       # Flutter 工程（安卓 + macOS 共用一套代码）
│   ├── lib/main.dart                   # 入口：平台门槛（前台服务仅安卓）
│   ├── lib/pages/                      # home_page(含macOS桌面分支)/stats_page/settings_page
│   ├── lib/src/                        # engine(纯函数状态机)/models/storage/theme/widgets
│   │                                  # cloud_sync/cloud_auto(腾讯云同步)/notifications
│   │                                  # menu_bar_timer(macOS菜单栏)/sheets
│   ├── android/  macos/                # 双端壳；macOS 含 NSStatusItem 菜单栏计时
│   └── test/                           # 19 项测试（计时/同步/迁移/UI渲染）
├── tencent-sync/                       # 腾讯云 SCF+COS 同步后端（deploy.py，已关CLS投递）
├── cloudflare-reminders/               # 网页端 Web Push 后端（Cloudflare DO）
├── github-pages/  work/                # 网页辅助文件/临时工作
├── tools/                              # flutter、android-sdk（本地工具链，勿动）
└── outputs/
    ├── 松果-v1.1.0-arm64-锁屏倒计时-系统走表.apk   # ★ 安卓最新安装包
    ├── 松果-专注时光-macos.app / .zip              # ★ macOS 桌面版
    └── archive/                                     # 历史 APK/网页备份（归档勿用）
```

## 2. 三端功能状态

| 功能 | 网页版 | 安卓 | macOS 桌面 |
|---|---|---|---|
| 番茄钟计时（专注/短休/长休/超时累计） | ✅ | ✅ | ✅ |
| 任务清单 / 今日清单 / 补记 | ✅ | ✅ | ✅ |
| 今日目标（**分钟**，旧「次」自动迁移） | ✅ | ✅ | ✅ |
| 统计：周图表 / **本月回顾** / 连续天数 | 周视图 | ✅ | ✅ |
| 云端同步（腾讯云，同码互通） | ✅ | ✅ | ✅ |
| 到点提醒 | Web Push | 本地通知 | ⚠️ 未实机验证 |
| 锁屏/后台显示剩余时间 | — | 系统 Chronometer 通知 | 菜单栏 🍃+倒计时 ⚠️ 待确认 |
| 手机刷新不重置 / 桌面快捷键(空格/R) | ✅(刷新恢复) | 系统级 | ✅ |

## 3. 近期进展（2026-08-31 → 09-06）

1. **网页+安卓**：今日目标从「次」改为「分钟」（含旧数据迁移、云端净荷/指纹同步改字段）
2. **网页**：分钟记录全部取整；专注/休息刷新页面均不重置（补上休息恢复）
3. **安卓 UI**：新版暗色「专注仪表」布局；统计页周图
4. **安卓锁屏计时**：前台通知由服务每秒刷新 → 改为**系统 Chronometer 渲染**（进程被 ColorOS 冻结时仍走表）；锁屏只显示时间不泄露任务名
5. **macOS 版**：突破 CocoaPods/沙箱两个构建障碍，可本地构建运行；独立桌面 UI（侧边栏+双栏）、本月回顾、菜单栏剩余时间
6. **腾讯云欠费处理**：根因 CLS 日志分区费；已关闭函数日志投递（deploy.py 固化）、删除遗留日志集/主题；欠费金额待用户充值结清

## 4. 当前待办与已知问题

| # | 事项 | 状态/操作 |
|---|---|---|
| 1 | **macOS 菜单栏计时收尾** | 通道已确认通畅（收到实时倒计时）；诊断版残留日志/静态标题/启动测试消息，**待清理后出正式版**（代码未提交） |
| 2 | **清理诊断代码** | `main.dart` 启动测试 Timer、`MainFlutterWindow.swift` 文件日志+初始"等待消息"、`menu_bar_timer.dart` 异常日志 → 清掉重新构建 |
| 3 | **腾讯云充值** | 欠费结清 + 设余额预警（CLS 已止血，充值主要保护 COS 云端备份） |
| 4 | **推送未同步提交** | 本地领先远端约 7 个提交（macOS 全系列 + 文档），需代理（Clash :7890）后 `git push`；工作区 `.git/index.lock` 残留会导致推送报错，先 `mv .git/index.lock /tmp/pine-git-locks/` |
| 5 | **安卓真机验证锁屏倒计时** | 设备 OPPO PLG110 已装「系统走表」版；需完整验证：开始计时→锁屏→倒计时持续走动（ColorOS 冻结时依赖系统渲染，理论已解决） |
| 6 | 网页端历史小数数据 | 已在加载时清洗为整数，需强刷确认显示 |
| 7 | macOS 到点提醒 | 代码路径存在，未经实机验证（需系统授权通知） |

## 5. 产物速查

| 产物 | 说明 |
|---|---|
| `outputs/松果-v1.1.0-arm64-锁屏倒计时-系统走表.apk` | 安卓最新（新版UI+锁屏Chronometer） |
| `outputs/松果-专注时光-macos.zip` | macOS 桌面版（双击解压运行） |
| `outputs/archive/` | 历史包归档（自动同步/新版UI/锁屏计时等旧版） |
| 网页 | `https://syc13534102408-gif.github.io/pomodoro/` |

## 6. 常见操作速记

- 测试/分析：`cd pomodoro_app && ../tools/flutter/bin/flutter test` / `flutter analyze`
- 安卓 APK / macOS 构建：**先 rsync 到 /tmp**（工作区写保护），命令见交接文档 5.2 / 5.5
- 云端自检：`curl https://1478270344-dtbqag7t8b.ap-guangzhou.tencentscf.com/ping`
