# 02 技术栈与目录结构

## 1. 技术栈

| 端 | 技术 | 版本/说明 |
|---|---|---|
| 网页版 | 原生 HTML/CSS/JS 单文件 | `index.html`（约 74KB，含全部 CSS/JS），无框架、无构建步骤 |
| 网页版提醒 | Service Worker | `sw.js`（接收 Web Push） |
| 安卓/macOS | Flutter 3.47.1 stable，Dart 3.13.1 | SDK 在 `tools/flutter/`（项目内，不随 Git 提交） |
| 安卓构建 | Gradle Kotlin DSL + AGP 9.1.0 + Kotlin 2.4.0 | JDK 17（`tools/jdk/jdk-17.0.13.jdk`） |
| 同步后端 | 腾讯云 SCF，Node.js 18.15，128MB / 超时 20s | `tencent-sync/src/index.js`，存储用 COS |
| 提醒后端 | Cloudflare Workers + Durable Objects + D1 | `cloudflare-reminders/` |

### 1.1 Flutter 依赖（`pomodoro_app/pubspec.yaml`，实测）

| 包 | 用途 |
|---|---|
| `shared_preferences` | 本地持久化 |
| `intl` | 时间/数字格式化 |
| `flutter_local_notifications` + `timezone` | 本地通知 |
| `flutter_foreground_task` | 前台服务常驻（仅安卓） |
| `http` | 云端同步请求 |
| `audioplayers` | 完成提示音（`assets/sounds/`） |
| `flutter_lints`（dev） | 静态检查 |

- `dependency_overrides: path_provider_android: 2.2.23` **必须保留**：2.3.x 引入的 `jni` 原生模块会把 CMake 产物写回只读 pub 缓存导致构建失败。
- `fl_chart` 已于 2026-09-07 移除（统计图改为 `BrickBars` 自绘），**禁止重新引入**。

### 1.2 Android 构建配置（`pomodoro_app/android/app/build.gradle.kts`）

- `applicationId = com.example.pomodoro_app`（仍为模板默认值，未定制）
- `minSdk/targetSdk/compileSdk` 均取 `flutter.*` 模板默认值，未显式指定
- `versionCode/versionName` 取自 `pubspec.yaml`（当前 4 / 1.2.0）
- **release 使用 debug 签名**（`signingConfig = signingConfigs.getByName("debug")`）
- 开启 core library desugaring（`desugar_jdk_libs:2.1.5`），Java/Kotlin 均为 JVM 17

## 2. 目录结构

```
pinecone/
├── README.md                  # ★ 文档地图 + 快速开始（先读这个）
├── CHANGELOG.md               # 版本时间线 + bugfix 根因速记 + 后续建议
├── docs/                      # ★ 项目文档（本文档所在处）
│   ├── 01~07-*.md             # 七章权威文档
│   ├── design/                # UI 设计规格与实施记录
│   └── archive/               # 历史快照（不再维护，勿作为事实来源）
│
├── index.html                 # ★ 网页版全部代码（单文件，含内联 CSS/JS）
├── sw.js                      # 网页端 Service Worker
│
├── pomodoro_app/              # ★ 安卓 + macOS 共用的 Flutter 工程
│   ├── pubspec.yaml           # 依赖清单（见 1.1）
│   ├── analysis_options.yaml  # flutter_lints 3
│   ├── lib/
│   │   ├── main.dart          # 入口：安卓初始化前台服务端口 → HomePage
│   │   ├── pages/
│   │   │   ├── home_page.dart     # 计时页（含 macOS 桌面双栏布局分支）
│   │   │   ├── stats_page.dart    # 统计页（周分布 + 本月回顾 + 事件构成）
│   │   │   └── settings_page.dart # 设置页（四分区）
│   │   ├── src/
│   │   │   ├── engine.dart        # ★ 计时状态机（纯函数）
│   │   │   ├── models.dart        # ★ 数据模型 + 云净荷
│   │   │   ├── storage.dart       # 本地持久化 + 云合并
│   │   │   ├── cloud_sync.dart    # 云同步 HTTP 客户端
│   │   │   ├── cloud_auto.dart    # 自动同步 + 冲突弹窗 + 指纹
│   │   │   ├── notifications.dart # 本地通知 + 前台服务 + 锁屏倒计时
│   │   │   ├── theme.dart         # ★ 设计令牌（扁平积木）
│   │   │   ├── widgets.dart       # ★ 积木组件库（12 项）
│   │   │   ├── sheets.dart        # 三个底栏弹窗
│   │   │   ├── menu_bar_timer.dart# macOS 菜单栏 + 悬浮窗状态推送
│   │   ├── android/  macos/   # 双端壳工程（macOS 侧含菜单栏 NSStatusItem、悬浮计时 NSPanel）
│   │   ├── test/              # 56 项测试 / 7 个文件（见 03）
│   │   └── tool/gen_icons.py  # 启动器图标生成脚本（Python）
│   └── build/                 # 构建产物（gitignore）
│
├── tencent-sync/              # 腾讯云同步后端（deploy.py 部署）
├── cloudflare-reminders/      # 网页端 Web Push 后端（wrangler 部署）
├── github-pages/              # 网页版初始发布快照（仅存档，实际发布用仓库根目录）
├── work/                      # 空目录（临时工作区，未使用）
├── outputs/                   # 交付物（gitignore，不入库）
│   ├── *.apk                  # 安卓安装包
│   ├── 松果-v1.2.0-*.apk
│   ├── 松果-专注时光-macos.app / 松果-专注时光-macOS-悬浮计时-2026-09-09.zip
│   └── archive/               # 历史安装包（勿用）
│
└── tools/                     # 本机工具链（gitignore，勿删）
    ├── flutter/               # Flutter SDK 3.47.1
    ├── android-sdk/           # Android SDK（platform-tools/adb、build-tools 36.0.0）
    ├── jdk/jdk-17.0.13.jdk    # JDK 17
    └── downloads/
```

> `outputs/` 与 `tools/` 在 `.gitignore` 中，克隆仓库后不存在，需另行获取或重建工具链。

## 3. 入口文件速查

| 想做的事 | 看哪里 |
|---|---|
| 跑起来（安卓） | `pomodoro_app/lib/main.dart` → `lib/pages/home_page.dart` |
| 改计时规则 | `lib/src/engine.dart`（纯函数，配套 `test/widget_test.dart`） |
| 改数据结构 | `lib/src/models.dart`（所有类都有 `copyWith`/`toMap`/`fromMap`） |
| 改同步 | `lib/src/cloud_sync.dart`（接口）+ `lib/src/cloud_auto.dart`（策略） |
| 改视觉 | `lib/src/theme.dart`（令牌）→ `lib/src/widgets.dart`（组件）→ 各页面 |
| 改弹窗 | `lib/src/sheets.dart` |
| 改通知/锁屏 | `lib/src/notifications.dart` |
| 改 macOS 菜单栏 | `lib/src/menu_bar_timer.dart` + `macos/Runner/MainFlutterWindow.swift` |
| 改 macOS 悬浮计时窗 | `macos/Runner/FloatingTimerPanel.swift`（渲染与拖动）+ `menu_bar_timer.dart`（推状态） |
| 改网页端 | `index.html`（单文件内分区注释） |
| 改同步后端 | `tencent-sync/src/index.js` + `tencent-sync/deploy.py` |
| 改启动器图标 | `pomodoro_app/tool/gen_icons.py` + `android/app/src/main/res/drawable/` |
