# 松果番茄钟（Pomodoro）项目交接文档

> 最后更新：2026-09-01 ｜ 接手者阅读本文档后应能独立理解、运行与继续开发本项目。
> 仓库：`github.com/syc13534102408-gif/pomodoro`（分支 `main`）

---

## 1. 项目背景与目标

**项目名称**：松果 / 专注时光（Pomodoro Timer）

**背景**：用户需要一个「本地优先」的番茄钟工具，核心诉求是**数据在自己手里、断网可用**，同时支持多设备互通。项目最早从网页版起步，随后补齐了安卓原生 App 和云端同步，形成了「网页 + 安卓 + 云端」三端体系。

**目标**：
- 计时：专注 / 短休息 / 长休息，到点后**不自动结束**，继续累计超时，由用户确认完成（deadline + overtime 模型）
- 本地优先：所有数据存本机（浏览器 localStorage / App SharedPreferences），云端只做备份与跨设备搬运
- 互通：网页端与安卓端共用同一套云端同步，完成专注后自动上传、回前台自动拉取
- 提醒：网页端支持 Web Push 后台提醒；安卓端用本地通知 + 前台服务常驻

**当前状态**：网页版已上线；安卓 App 功能完整；云端同步已从 Cloudflare 迁移至腾讯云（因 `workers.dev` 国内网络不可达）。

**对外访问地址**：
| 端 | 地址 |
|---|---|
| 网页版 | `https://syc13534102408-gif.github.io/pomodoro/`（GitHub Pages） |
| 云端同步（腾讯云） | `https://1478270344-dtbqag7t8b.ap-guangzhou.tencentscf.com` |
| 后台提醒（Cloudflare） | `https://pine-pomodoro-reminders.pine-pomodoro-reminders.workers.dev`（仅网页端推送用） |
| 安卓 App | 发布包见 `outputs/松果-v1.1.0-arm64-自动同步.apk`（versionName 1.1.0） |

---

## 2. 主要功能模块

### 2.1 计时核心（网页 + 安卓共享同一套模型）
- 三种模式：专注（默认 25 分钟）、短休息（5）、长休息（15），可在设置中调整
- **到点继续计时**：计划时长结束后不自动结束，显示 `+mm:ss` 超时，直到用户点「完成」才落记录
- 每完成 1 次专注计 1 个番茄；**每第 4 个番茄自动进入长休息**，其余进入短休息
- 专注结束后自动开始休息计时；休息不产生记录
- 重置（丢弃）不写统计；暂停冻结剩余时间，继续后从原剩余值恢复
- 任务（事件）体系：可增删改任务、给任务配色、按任务聚合统计

### 2.2 统计
- 今日分钟 / 本周分钟、今日目标（**专注分钟**，默认 200 分钟；2026-09-01 起由「次」改为「时长」，旧数据按 次数 × 专注时长 自动迁移，字段名 `goalTarget` → `goalMinutes`）、本周目标（默认 12 次，仍按次计）
- 本周按周一到周日聚合的堆叠柱状图，按任务分色；连续天数
- 最近完成记录列表、手动补记（可指定事件/分钟/时间）

### 2.3 云端同步（互通核心）
- **同步码（deviceCode）机制**：16–128 位 `[A-Za-z0-9_-]`，**云端按同步码存一份备份**。两端互通的前提是使用**同一个同步码**（在网页端「查看同步码」复制，手机端「从云端恢复」输入）
- 接口（腾讯云与 Cloudflare 两套后端接口完全一致）：
  | 方法 | 路径 | 说明 |
  |---|---|---|
  | POST | `/sync/upload` | body `{deviceCode, payload, baseUpdatedAt?}` → `{updatedAt}`；`baseUpdatedAt` 与云端不一致返回 **409** |
  | GET | `/sync/download?deviceCode=` | → `{payload, updatedAt}`；无备份返回 404 |
  | GET | `/ping` | 自检：腾讯云返回 `provider: tencent-scf` |
- **网页端自动同步**：设置里开启「自动双向同步」后，页面打开期间每 45 秒对比一次；切回标签页立即同步
- **安卓端自动同步**（`lib/src/cloud_auto.dart`）：完成专注后自动上传；App 启动/回前台自动拉取；冲突（两端都有改动）弹窗让用户选择，**绝不静默覆盖**
- 指纹机制：本机用「规范化云净荷」指纹（剔除每次恢复都会重新生成的 id 字段）判断自上次同步后是否有改动

### 2.4 提醒
- **网页端**：Web Push（VAPID + Service Worker）——计划结束时由 Cloudflare Worker 的 Durable Object Alarm 精确定时推送，页面关闭也能提醒
- **安卓端**：本地通知（`flutter_local_notifications`）+ 前台服务常驻（`flutter_foreground_task`）保活；锁屏倒计时由 `LockTimer`（`lib/src/notifications.dart`）用**系统 Chronometer** 渲染（2026-09-01 起），SystemUI 直接走表、不依赖 App 进程存活——ColorOS 冻结进程时倒计时仍在走；超时后改为从超时起点累计；到点提醒仍是本地通知，不依赖云端

### 2.5 数据与备份
- 导出/导入 JSON 备份、导出 CSV 记录、恢复前自动下载本机安全副本
- 云端备份**不包含**推送订阅、进行中的临时计时、同步状态本身

---

## 3. 目录结构及各目录/文件用途

```
wo-xi/
├── index.html                  # 网页版（单文件应用，含全部 CSS/JS）
├── sw.js                       # 网页端 Service Worker（接收 Web Push）
├── README.md                   # 根说明：部署方式 + 同步后端现状 + 构建绕法
├── .gitignore
│
├── github-pages/               # 网页版发布包快照（历史用途；当前直接由根目录 index.html 发 Pages）
│   ├── index.html
│   └── sw.js
│
├── pomodoro_app/               # ★ 安卓 App（Flutter）
│   ├── pubspec.yaml            # 依赖清单（见第 4 节）
│   ├── lib/
│   │   ├── main.dart           # 入口：初始化前台服务通信端口，启动 HomePage
│   │   ├── pages/
│   │   │   ├── home_page.dart      # 主界面：计时环、模式切换、任务/待办/记录列表（601 行）
│   │   │   ├── settings_page.dart  # 设置：计时参数、云端同步（上传/恢复/同步码）、数据管理
│   │   │   └── stats_page.dart     # 统计页：本周堆叠柱状图、按事件累计
│   │   └── src/
│   │       ├── engine.dart         # ★ 计时状态机（纯函数，可单测）：start/pause/resume/advance/complete/discard
│   │       ├── models.dart         # ★ 数据模型：AppData/PineTask/FocusRecord/TodoItem/ActiveSession/SyncState/TimerSettings
│   │       ├── storage.dart        # 本地持久化（SharedPreferences）+ mergeFromCloud 合并
│   │       ├── cloud_sync.dart     # 云端同步客户端（HTTP）：upload/download，defaultBaseUrl 指向腾讯云
│   │       ├── cloud_auto.dart     # ★ 自动同步：完成专注自动上传、回前台自动拉取、冲突弹窗、指纹算法
│   │       ├── notifications.dart  # 本地通知 + 提示音
│   │       ├── sheets.dart         # 底部弹层：任务管理/待办/记录明细
│   │       ├── theme.dart          # 主题（松果配色）
│   │       └── widgets.dart        # 复用组件（环形计时器 RingTimer、面板等）
│   ├── test/
│   │   ├── widget_test.dart        # 引擎 + 界面测试（7 个）
│   │   └── cloud_auto_test.dart    # 自动同步逻辑测试（10 个）
│   ├── android/                    # Android 工程（manifest 含 INTERNET/前台服务声明）
│   ├── assets/sounds/chime.wav     # 完成提示音
│   └── macos/                      # macOS 壳（仅用于本机预览验证，无 CocoaPods 无法构建）
│
├── cloudflare-reminders/        # Cloudflare Worker（当前仅剩：网页端后台提醒 + 旧同步后端）
│   ├── src/index.js             # Worker 逻辑：Web Push 订阅/精确定时提醒；同步接口（保留但已不再使用）
│   ├── schema.sql               # D1 表结构（sync_backups / request_log 诊断表）
│   ├── wrangler.jsonc           # Worker 配置：Durable Object TimerReminder + D1
│   └── package.json
│
├── tencent-sync/                # ★ 腾讯云同步后端（当前线上同步服务）
│   ├── src/index.js             # SCF 函数：Node.js 18，读写 COS（接口见 2.3）
│   ├── deploy.py                # 部署脚本（tencentcloud-sdk-python-scf）：打包→建/更新函数→HTTP 触发器→输出地址
│   ├── README.md                # 部署与架构说明（必读）
│   ├── package.json             # cos-nodejs-sdk-v5
│   └── .last-url                # 当前线上函数 URL（gitignore）
│
├── outputs/                     # 交付物（gitignore）
│   ├── 松果-v1.1.0-arm64-自动同步.apk   # ★ 最新安卓安装包（19.1MB, arm64）
│   ├── 松果-v1.1.0-arm64-腾讯云同步.apk # 上一版（手动同步，无自动同步）
│   ├── pomodoro.zip / *.html / sw.js   # 网页版发布包
│   └── ui-previews/             # 界面预览截图
│
└── tools/                       # 本机构建工具链（gitignore，勿删）
    ├── flutter/                 # 项目内 Flutter SDK（本地安装）
    ├── android-sdk/             # Android SDK（含 platform-tools/adb、build-tools）
    ├── jdk/                     # JDK
    └── downloads/
```

---

## 4. 技术栈与依赖

### 4.1 网页版
- 原生 HTML/CSS/JS 单文件（无框架、无构建步骤），227 行含全部逻辑
- Service Worker（`sw.js`）+ Web Push API

### 4.2 安卓端（Flutter）
- Flutter（SDK 在项目内 `tools/flutter/`），Dart SDK ≥3.3
- 依赖（`pubspec.yaml`）：
  | 包 | 用途 |
  |---|---|
  | shared_preferences | 本地持久化 |
  | fl_chart | 统计图表 |
  | intl | 时间/数字格式化 |
  | flutter_local_notifications + timezone | 本地通知 |
  | flutter_foreground_task | 前台服务常驻（保活 + 常驻通知） |
  | http | 云端同步 HTTP 请求 |
  | audioplayers | 完成提示音 |
  - `dependency_overrides: path_provider_android: 2.2.23`：**必须保留**——2.3.x 引入的 `jni` 原生模块会把 CMake 产物写回只读 pub 缓存导致构建失败
- 包名 `com.example.pomodoro_app`；minSdk 24；仅构建 `arm64-v8a`（目标机 OPPO Find X9 Pro）

### 4.3 云端同步（腾讯云）
- **SCF 云函数**：Runtime Node.js 18.15，128MB，超时 20s，入口 `index.main_handler`
- **COS 对象存储**：桶 `pine-sync-1478270344`（ap-guangzhou，私有读写），键 `sync/{deviceCode}.json`
- **函数 URL 触发器**（免鉴权，CORS 由函数代码处理）：`https://1478270344-dtbqag7t8b.ap-guangzhou.tencentscf.com`
- 部署脚本：Python `tencentcloud-sdk-python-scf`（纯 wheel）；密钥从 `~/.tencentcloud/credentials` 读取（`TENCENTCLOUD_SECRET_ID/KEY` 两行，chmod 600）
- 函数环境变量：`COS_BUCKET`、`COS_REGION`、`ALLOWED_ORIGINS`、`PINE_SECRET_ID`、`PINE_SECRET_KEY`（**注意用 `PINE_` 前缀**，SCF 保留 `TENCENTCLOUD_` 前缀给系统变量）

### 4.4 后台提醒（Cloudflare）
- Workers + Durable Object（`TimerReminder`，Alarm 精确定时）+ D1（`pine-pomodoro-sync`）
- Secrets：`VAPID_PUBLIC_KEY`、`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT`、`ALLOWED_ORIGIN=https://syc13534102408-gif.github.io`
- **VAPID 私钥只能存 Cloudflare Secret，绝不能提交进 Git / GitHub Pages**

---

## 5. 运行与启动方式

### 5.1 网页版
- 本地预览：直接用浏览器打开 `index.html`（无需服务器）
- 线上：推送到 GitHub `main` 分支后，Pages 自动从仓库根目录发布：
  ```bash
  cd wo-xi
  git add -A && git commit -m "..." && git push origin main
  # 访问 https://syc13534102408-gif.github.io/pomodoro/
  ```
  > 注意：本机 shell 可能带代理（`http_proxy=127.0.0.1:7890`），push 报连接失败时用
  > `env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY git push origin main`

### 5.2 安卓 App
**重要：本机工作区有删除/写入保护，直接 `flutter build apk` 会在 `.dart_tool/hooks_runner` 与 `build/reports` 报 `Unable to open file ... for writing snapshot` / `Operation not permitted`，必须复制到 `/tmp` 构建**：
```bash
rm -rf /tmp/pine-app && mkdir -p /tmp/pine-app
rsync -a --exclude build --exclude .dart_tool --exclude .gradle pomodoro_app/ /tmp/pine-app/
cd /tmp/pine-app
# 同步最新代码后再构建：
rsync -a /path/to/wo-xi/pomodoro_app/lib/ /path/to/wo-xi/pomodoro_app/test/ /tmp/pine-app/lib/ /tmp/pine-app/test/

# 静态分析 + 测试（工作区内即可运行，不会触发构建保护）：
/path/to/wo-xi/tools/flutter/bin/flutter analyze
/path/to/wo-xi/tools/flutter/bin/flutter test

# 构建 arm64 安装包：
/path/to/wo-xi/tools/flutter/bin/flutter build apk --release --target-platform android-arm64
# 产物：/tmp/pine-app/build/app/outputs/flutter-apk/app-release.apk → 拷回 outputs/
```
- 安装到手机（USB 调试）：
  ```bash
  tools/android-sdk/platform-tools/adb install -r outputs/松果-v1.1.0-arm64-自动同步.apk
  ```

### 5.3 腾讯云同步后端（重部署）
```bash
cd tencent-sync
# 密钥文件 ~/.tencentcloud/credentials 需已存在（格式见 4.3）
COS_BUCKET=pine-sync-1478270344 \
  /Users/shuyichen/.workbuddy/binaries/python/envs/default/bin/python deploy.py          # 创建/更新
COS_BUCKET=pine-sync-1478270344 \
  /Users/shuyichen/.workbuddy/binaries/python/envs/default/bin/python deploy.py --update   # 只更新代码
curl "$(cat tencent-sync/.last-url)/ping"   # 期望 {"ok":true,"provider":"tencent-scf",...}
```
> 注意：`--update` 会**整体替换**环境变量，脚本已保证 `PINE_SECRET_*` 始终带上；若手动改配置请务必保留密钥变量。

### 5.4 Cloudflare Worker（仅当需要动后台提醒时）
```bash
cd cloudflare-reminders
npm install && npx wrangler login
npx wrangler d1 execute pine-pomodoro-sync --remote --file=schema.sql   # 首次
npx wrangler secret put VAPID_PUBLIC_KEY   # 等 4 个 Secret 逐个设置
npm run deploy
```

---

## 6. 关键设计说明（改代码前必读）

### 6.1 计时模型（`engine.dart`，纯函数状态机）
- 所有状态在 `AppData` 上，所有操作返回新对象，不就地修改 → 可单测
- **超时锚点**：到点后 `overtimeStartedAt` 取**计划结束时刻**（`session.deadline`）而非"现在"，保证后台期间经过的时间也计入超时（历史 bug，有单测守护）
- `complete()` / `discard()` 用 `copyWith(clearActiveSession: true)` 清会话（`??` 无法置空，需显式 flag）
- 只有专注模式写 `FocusRecord`；休息不污染记录表
- 每第 4 个完成 → 长休息

### 6.2 同步模型（`cloud_auto.dart` + 网页端 autoSync）
- 同一同步码 = 同一份云端备份；两端的云净荷 schema 不同（网页：`tasks/records/history/done`；安卓：`tasks/records/todos/settings/...`），字段不齐时以缺省补齐
- 冲突：上传带 `baseUpdatedAt`（上次同步时刻），云端不匹配返回 409 → 转拉取 → 两端都有改动时**弹窗询问**，无改动时静默采用云端
- 指纹：`cloudFingerprint()` 是**规范化**净荷的 JSON（剔除 id），用于判断"本机自上次同步后是否有改动"——id 由时间戳生成，每次从云端恢复都会变，若不剔除会产生假冲突（已有回归测试）
- 安卓自动同步只在**已绑定同步码**时工作，避免静默创建网页端看不到的孤立备份

### 6.3 冲突与数据安全
- 恢复前自动下载本机安全副本（`pine-local-before-restore-*.json`）
- 云端 409 不会静默覆盖；`lastSyncedHash` 记录上次同步指纹

---

## 7. 尚未完成或需要注意的事项

### 7.1 待办
- [ ] **手机真机验证**：新 APK（自动同步版）尚未 `adb install` 到 OPPO Find X9 Pro 做端到端验证（手机需开 USB 调试连电脑）
- [ ] **首次互通迁移**：云端已从 Cloudflare 换到腾讯云，**新旧数据不互通**。流程：网页版设置→上传本机数据（写入腾讯云）→ 手机「从云端恢复」输入网页同步码。之后自动同步即可工作
- [ ] 用户偏好类问题未定：称呼、城市（可选，影响问候语等本地化）

### 7.2 已知限制 / 环境问题
1. **本机构建限制**：工作区删除/写入保护导致 `flutter build apk` 必须在 `/tmp` 副本进行（见 5.2）；`git` 的 `index.lock` 也常被保护拦，报错时 `mv .git/index.lock /tmp/pine-git-locks/` 后重试
2. **macOS 桌面端无法构建**：无 CocoaPods（系统 Ruby 2.6 缺 Xcode headers）；界面验证只能在真机做，Web 截图/Golden 测试不可用
3. **`workers.dev` 国内不可达**（已实测手机网络）：Cloudflare 同步接口在国内移动网络不可用——这是迁移腾讯云的原因；**网页端后台提醒仍依赖 Cloudflare**，国内网络下网页端后台提醒可能失效（需自行判断是否可接受）
4. **OPPO/ColorOS 后台冻结**：系统进程 `OplusHansManager` / `LocationFreezeProc` 会冻结后台进程，可能影响前台服务保活与通知到达，必要时需引导用户加白名单/自启动
5. **网页端自动同步的跨端 schema 差异**：网页与安卓净荷字段不完全一致，网页端在检测到手机端上传时可能频繁弹"另一台设备更新"询问；如需根治可统一两端净荷 schema（工作量大，暂缓）
6. **腾讯云侧**：COS 桶需在控制台手动创建（SDK 不含桶管理）；函数 URL 免鉴权（依赖业务层 400/409 校验）；API 网关已停服（2025-06-30），**不要用 API 网关**
7. **费用**：SCF 每月 100 万次请求 + COS 50GB 免费额度内，个人使用无忧。**CLS 日志费用已于 2026-09-01 根治**：函数日志投递到 CLS 后，日志主题即使几乎无写入也按分区费持续扣费（曾因此累积小额欠费、CLS 停服，但不影响同步功能）；现已关闭函数日志投递并删除遗留日志集/主题，`deploy.py` 每次部署都会自动关闭（见 `detach_cls_logs`）。建议在腾讯云设置余额预警

### 7.3 修改时的雷区
- `pubspec.yaml` 的 `path_provider_android: 2.2.23` override **勿删**（见 4.2）
- 函数环境变量密钥必须用 `PINE_` 前缀（`TENCENTCLOUD_` 被 SCF 保留）；更新函数配置会整体替换环境变量，务必带上密钥
- `deploy.py` 里的 `detach_cls_logs` **勿删**：每次部署自动关闭日志投递，否则 SCF 会重新挂载日志主题，CLS 按分区持续扣费（见 7.2.7）
- `cloud_auto.dart` 的指纹算法改动需同步更新 `test/cloud_auto_test.dart` 的回归用例（id 重生成不产生假冲突）
- **锁屏倒计时通知（2026-09-01 起）**：由 `LockTimer`（`lib/src/notifications.dart`）发布，与前台服务通知**共用 id 5150、频道 pine_lock_timer**，发布即原地替换，不要改成两个通知；也不要重新引入"每秒刷新通知标题"的逻辑（`_syncForeground` 只在状态切换时更新）——那会打断 Chronometer 走表，且进程被冻结时刷新本身无效。剩余/超时的切换只发生在 `targetReached` 状态翻转时
- 改 `index.html` 的同步地址时同时改 `pomodoro_app/lib/src/cloud_sync.dart` 的 `defaultBaseUrl`，两端保持一致

---

## 8. 常用命令速查

| 操作 | 命令 |
|---|---|
| 全量测试 | `cd pomodoro_app && ../tools/flutter/bin/flutter test` |
| 静态分析 | `../tools/flutter/bin/flutter analyze` |
| 构建 APK | 见 5.2（先复制到 /tmp） |
| 装到手机 | `tools/android-sdk/platform-tools/adb install -r outputs/松果-v1.1.0-arm64-自动同步.apk` |
| 云端自检 | `curl https://1478270344-dtbqag7t8b.ap-guangzhou.tencentscf.com/ping` |
| 重部署腾讯云 | 见 5.3（`deploy.py`） |
| 推网页 | `git push origin main`（代理失败时先 `env -u http_proxy ...`） |
